spring:
  datasource:
    username: postgres
    password: root123
    url: jdbc:postgresql://localhost:5432/snap_books
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
